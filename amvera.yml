---
meta:
  environment: jvm
  toolchain:
    name: gradle
    version: 22
build:
  artifacts:
    build/libs/*.jar: /
  args: spring-boot:repackage
run:
  jarName: demo-0.0.1-SNAPSHOT-plain.jar
  persistenceMount: /data
  containerPort: 8080
